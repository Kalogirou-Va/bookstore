{% extends "base.html.twig" %}

{% block body %}
    <div class="w-4/5 m-auto text-center">
        <h1 class="text-6xl block py-2 pr-4 pl-3 text-gray-700 border-b border-gray-100">
            Book catalog
        </h1>
    </div>

    <div class="md:grid lg:grid-cols-3 gap-20 w-4/5 mx-auto py-15 ">
        {% for author in authors %}
       
            <div class=" bg-gray-200">
                <div class="inline-block align-top align-center">
                    <img
                        src="{{ author.imagePath }}"
                        alt=""
                        class="h-30 w-25 "
                    />
               </div>
                <div class="inline-block pl-5 text-gray-700 font-bold text-sm py-2">
                    <ul class="list-disc">
                        <li>
                            <a href="/author/{{author.id}}" class=" hover:text-gray-500 hover:text-base">{{ author.name }}</a>
                        </li>
                        
                    </ul>
                </div>
            </div>

        {% endfor %}
    </div>
{% endblock %}